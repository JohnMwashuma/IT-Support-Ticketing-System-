@model ITSUPPORTTICKETSYSTEM.Areas.Admin.ViewModels.Tagsform

@using (Html.BeginForm("Index", "Tags"))
{
      if (!Model.IsNew)
    {
        @Html.HiddenFor(f => f.TagId)

    }
    
    @Html.AntiForgeryToken()
  
    
         @Html.ValidationSummary()
    <div class="form-group">
        <label><strong>Add new ticket tags below</strong></label>
        <p>
            @Html.TextAreaFor(f => f.Name, new { @class = "form-control",@rows="3", @placeholder = "e.g network issues,server issues, user problems", @id = "name" })
            @*<textarea class="form-control" rows="3" placeholder="e.g network issues,server issues, user problems"></textarea>*@
        </p>
        <p>
            <input type="submit" value="Create Tag" class="btn btn-primary btn-sm" />
        </p>
    </div>
                       
}

@section Scripts
{
    @Scripts.Render("~/admin/ticket/scripts")
   
}